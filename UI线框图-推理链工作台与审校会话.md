# UI 线框图：推理链工作台 & 审校会话

> 对应《MVP产品方案-深度分析工作台》第 11.5 / 11.6 节的可视化落地。
> 设计原则：过程可见（每步可展开、可追问、可反驳）、双向互动（人可仲裁、可质询）、说服力（敢给反证、分级阅读）。

---

## 1. 整体信息架构：三栏工作台

**布局**：左栏 = 推理链（纵向节点）｜中栏 = 当前步骤详情 ｜ 右栏 = 检查器（证据 / 置信度 / 批注）。
**顶部栏** = 项目、模式切换（中性 / 对抗）、保存、导出报告。

```
┌──────────────────────────────────────────────────────────────────────────┐
│  [PROJECT] RuiXing vs Cudi 9.9     [MODE: NEUTRAL]   [SAVE]  [EXPORT]    │
├──────────────────┬──────────────────────────────────┬────────────────────┤
│ REASONING CHAIN  │  STEP DETAIL                     │  INSPECTOR         │
│                  │                                  │  EVIDENCE          │
│ 1 Material/Source│  [Step 2 Anomaly Lock]           │  - 2.2B fraud      │
│    [done]        │  Baseline:                       │    independent[HI] │
│    |             │  "fraud firm should die"         │  - Cudi founded    │
│ 2 Anomaly Lock   │  Candidates (3):                 │    independent[HI] │
│    [active]      │   A. should-die but #1  <-pick   │  - 2026 status     │
│    |             │   B. entrant starts price war    │    unverifiable[!] │
│ 3 Skeleton       │   C. same founder two products   │                    │
│    [pending]     │  [RESCAN]  [USE A AS ENTRY]      │  CONFIDENCE        │
│ 4 Mechanism      │                                  │  leverage 5/5      │
│ 5 Game/Analogy   │                                  │  baseline mid      │
│ 6 Scenarios      │                                  │                    │
│ 7 Adversarial QA │                                  │  NOTES             │
│                  │                                  │  [+] add note      │
│ [RECOMPUTE BELOW]│                                  │                    │
└──────────────────┴──────────────────────────────────┴────────────────────┘
```

**关键交互（对应产品机制）**：
- 点击任一节点 → 中栏显示该步骤详情；节点状态机：`待执行 → 进行中 → 已完成`，另有两种特殊状态：`用户已修改`（黄色）、`用户已否决`（红色）。
- 中栏任何论断可悬停 → 弹出"为什么这么判断" → 显示推导链 + 证据锚点（跳到右栏证据卡）。
- 右栏证据卡三档：`独立信源[强] / 单一信源[中] / 无法核实[!]`，无法核实强制标红——自反检验原则的产品化。
- "我不同意"按钮（每步详情右下角）→ 打开修订面板。

---

## 2. 推理链工作台：核心交互流程

### 2.1 修改传播（改一处，下游重算）

```
用户在中栏点 [我不同意] 于 步骤3 中性骨架
        ↓
弹出修订面板: 用户给出替代骨架命名 + 理由
        ↓
系统重生成步骤3; 步骤4/5/6/7 全部变为 "需重算" (黄色脏标记)
        ↓
用户点 [一键重算下游] 或 逐节点 [重算此步]
        ↓
每一步重算都会把 "修改原因 + 影响" 追加到审校记录
```

**为什么**：分析不是一次性输出，而是可迭代的活对象；下游必须跟随上游修正，否则报告自相矛盾。

### 2.2 "为什么走这条路"（游走理由显式化）

```
用户在 步骤5(博弈与类比) 点击节点 5.2 跨域类比
        ↓
中栏显示当前候选跳转 (3-5 个):
   A. 网约车大战  - 张力: 补贴由资本出
   B. 社区团购    - 张力: 补贴由平台出
   C. 共享单车    - 张力: 补贴由资本出
   D. 瑞幸2017-19 - 张力: 补贴由资本市场出
        ↓
每条都显示 "为什么是这个张力 -> 为什么指向这个案例"
        ↓
用户选/纠; 被选案例成为下一个骨架节点 (进入步骤3的新循环)
```

**为什么**：把"游走靠洞察"变成"系统给广度候选 + 用户凭洞察仲裁方向"——洞察仍然在人，但广度不再靠人的记忆。

### 2.3 异常点仲裁（多入口并行）

```
步骤2 生成 3-5 个候选异常, 每个附杠杆率评分
        ↓
不立即锁定; 系统提示: "先看你想先撕哪个口子"
        ↓
用户选一个(或系统按评分自动选), 其余候选保留在侧栏待切换
        ↓
若后续分析走偏, 用户可 [切换入口] 回到候选列表重撕
```

**为什么**：找错异常点 = 撕裂就错。多入口并行 + 可切换，用广度补偿单点洞察，且允许"撕错了重撕"。

---

## 3. 审校会话（交叉质询）

**布局**：左栏 = 报告锚点（点击定位到报告对应章节）｜右栏 = 质询对话。
**入口**：7 步流水线跑完后自动进入；也可从报告任意章节手动开启。

```
┌────────────────────────────────────────────────────────────────────────┐
│  REVIEW MODE  [report v1.3]  [disputes 2]  [EXPORT REVISED]  [CLOSE]   │
├──────────────────────────────────┬─────────────────────────────────────┤
│ REPORT ANCHORS                   │ INTERROGATION CHAT                  │
│                                  │                                     │
│ > 0 Material/Source              │ YOU:                                │
│ > 1 Anomaly                      │  "Conclusion 3 says Cudi will run   │
│ > 2 Skeleton                     │   out of cash, but RuiXing survived │
│ > 3 Mechanism                    │   a 2.2B fraud, why cant Cudi?"     │
│ > 4 Scenarios  [A 40%]           │ SYS: [CONCEDE] Strongest counter-   │
│ > 5 Core Claims [*interrogate]   │   argument; my reply covered 2/3.   │
│ > 6 Falsifiability               │   Re-checking franchisee finance..  │
│ > 7 Devil's Advocate             │   -> Revise Claim 3, conf 65%->50%  │
│ > 8 Meta-audit                   │                                     │
│ > 9 Evidence Table               │ YOU: "Why name the skeleton         │
│                                  │   'expand with other's money' not   │
│ [dispute: Claim 3]               │   'capital life-support'?"          │
│ [dispute: Skeleton name]         │ SYS: [DEFEND] naming is based on    │
│                                  │   capital source = franchisees,     │
│                                  │   not capital markets...            │
└──────────────────────────────────┴─────────────────────────────────────┘
```

### 3.1 系统的三种响应协议（质询必须落三种之一）

| 响应 | 触发 | 系统行为 | UI 反馈 |
|------|------|---------|--------|
| 辩护 | 质询可由现有证据链回答 | 展示推导链 + 证据锚点，不修改结论 | 绿色标记"已辩护" |
| 让步并修订 | 质询命中真实弱点 | 修订该结论/节点，更新置信度，下游重算 | 黄色标记 + 生成报告新版本 |
| 分歧挂起 | 证据不足且无法立即验证 | 标记为"未决问题"，写入报告待办 | 红色标记，计入审校记录 |

**硬约束**：系统不能"打太极"。质询要么辩护成功，要么修订，要么承认未决——三种都必须落进审校记录。

### 3.2 审校记录与报告版本

- 每次修订生成 `report v1.3 -> v1.4`，版本间可 diff。
- 审校记录本身成为报告的**附录**："本报告经受的质询"——这是说服力的一部分：**敢展示自己被怎么质疑、改了什么，比任何断言都更有说服力**。
- 审校结束后一键生成"审校摘要"（质询数 / 辩护数 / 修订数 / 未决数），可随报告一起导出。

---

## 4. 与 7 步流水线的对应

| 界面块 | 服务的步骤 | 补偿的瓶颈 |
|--------|-----------|-----------|
| 推理链（左栏） | 全流程过程可见 | "怎么走出来的"透明化 |
| 多入口并行（步骤2） | 异常锁定 | 找错异常点 |
| 游走候选池（步骤5） | 博弈与类比 | 游走方向靠洞察 |
| 修改传播（工作台） | 全流程可逆 | 一次性输出不可改 |
| 审校会话（右侧） | 对抗质检 + 结论定稿 | 结论说服力 |
| 检查器证据分级 | 全流程 | 幻觉/编造 |
| 审校记录/报告版本 | 输出侧 | 说服力 + 可审计性 |
