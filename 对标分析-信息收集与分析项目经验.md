# 对标分析：信息收集与分析项目 → 深度分析工作台

> 对标对象：`D:\AI项目\信息收集与分析`（Python 产品 `intel`，基于同一组 skill 解包后的产品化实现）。
> 结论先行：对方在「证据可信度」的工程化、入口完整度、快照存证与文档化上比我们完整；本文件记录学到的经验、已补项与待补项。

## 一、它做对了什么（我们缺什么）

1. **证据分级被工程化成规则代码（最大差距）**：S13「证据分级」在对方那里不是提示词里的一步，而是两条可执行规则：
   - 域名权威分级表（T1–T5：政府/官方 → 机构/学术 → 严肃媒体 → UGC/社交 → 未知），零 LLM、零成本、可解释。
   - 锚点提取 + 跨 ≥2 个独立域名才计「交叉印证」：用年份/数字/金额等事实锚点做一致性碰撞。
   - 效果：检索结果从「相关」升到「可信 + 可验」，这是产品化完整性的关键一环。
2. **交互：单框 + 深挖**：默认一次浅检索很便宜、不烧 token；点「深挖」才跑全流程分析，比「一次输入全跑」更省、更符合真实节奏。
3. **入口完整**：CLI + Web 双入口复用同一内核（我们此前的内核只在 server.js 内部，CLI 无法调用）。
4. **快照存证**：检索/分析结果带时间戳与原始引用，结论可复核。
5. **决策档案 / 采集层**：多引擎检索（DDG/Jina/Brave/OpenAlex）+ 正文抓取（trafilatura/Jina Reader）+ 分层方案文档。

## 二、我们的独特优势（它没有的）

1. **7 步推理流水线**：异常锁定 → 中性骨架 → 机制穿透 → 博弈类比 → 情景分支 → 对抗质检，是「怎么想到这个结论」的过程资产。
2. **审校三协议**：交叉质询必须落「辩护 / 让步并修订 / 挂起」，强制自我反驳，防止模型自嗨。
3. **轨迹卡交互**：把血淋淋案例压缩成「异常淘汰 → 撕裂 → 游走 → 碰壁 → 结论 + 可证伪条件」，是认知方法的产品化外壳。
4. **动态过程可见**：每一步游走与撕裂实时展示，结论带可证伪条件，比「给答案」更经得起追问。

## 三、本次已补（证据分级 + 交叉印证 + CLI 入口）

- `app/server.js`：新增 `sourceTier()`（域名 T1–T5 分级表）与 `corroborate()`（锚点提取、跨独立域名计数、权威分布），纯规则零 LLM。
- `/api/search` 返回每条结果的 `tier / tierLabel`，并附带 `summary`（独立域名数、权威分布、跨源锚点、逐域名明细）。
- `app/js/app.js`：结果渲染加分级徽标（T1–T5 五色）+ 底部「信源小结」+ 跨源锚点 chips；「加入材料」自动带上分级前缀。
- `app/cli.js`：命令行入口，与 Web 共用同一套检索/分级/印证内核（`node app/cli.js 查询词`）。
- `app/css/style.css`：`.tier.t1–t5` 徽标与 `.summary-box / .anchor-chip` 样式。

## 四、还待补（按价值排序）

1. **快照存证**：检索结果哈希 + 时间戳落库，结论可复核（P1）。
2. **决策档案**：每次分析结论、依据、用户改动存为档案，支持回溯命中率（P1）。
3. **「深挖」交互**：检索后先浅层给 8 条，点某条「深挖」再跑全流程，降低单次成本（P1）。
4. **采集层深化**：结果正文抓取（trafilatura 思路）+ 更多引擎（OpenAlex/Jina）备用（P2）。
5. **维度清单**：异常点 9 类 / 五要素 / 证据分级勾选做成可交互检查表（P2）。

## 五、一句话总结

它赢在「证据可信度」被工程化，我们赢在「认知过程」被可视化；本次已把它的制胜点（S13 规则化）补进我们的检索链路，让整套产品从「推理好看」到「结论可验」。